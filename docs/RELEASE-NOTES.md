Hub Tool 0.3.5: inventario ripulito dalle interfacce generiche e dai dispositivi software.

- Una sola voce Mouse e Tastiera al posto dei nodi HID generici duplicati.
- Escluse webcam virtuali, code OneNote/PDF/XPS/Fax e interfacce scanner duplicate.
- Stampanti e videocamere fisiche restano disponibili con i rispettivi controlli.
- `HubTool.exe` resta disponibile direttamente nella cartella principale e non richiede un'installazione separata di .NET.

Build e controlli automatici passati. L'EXE non è firmato Authenticode; i checksum SHA-256 sono inclusi. Consulta README per i limiti dei driver.
